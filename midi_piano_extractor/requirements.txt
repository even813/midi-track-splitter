# MIDI 钢琴分轨工具 - 依赖清单
# 安装命令：pip install -r requirements.txt

# ── 核心依赖 ──────────────────────────────────────────────
# MIDI 文件读写（轻量、纯 Python，无需系统 MIDI 驱动）
mido>=1.3.0

# ── 可选依赖 ──────────────────────────────────────────────
# 拖放支持（安装后可直接拖 MIDI 文件到窗口）
# Windows 用户推荐安装：
# tkinterdnd2>=0.3.0

# ── 标准库（无需安装）─────────────────────────────────────
# tkinter    - GUI（Python 自带）
# threading  - 多线程（Python 自带）
# pathlib    - 路径处理（Python 自带）
# re         - 正则表达式（Python 自带）
